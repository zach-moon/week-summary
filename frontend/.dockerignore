# 保持构建上下文精简，避免把本地产物/依赖塞进镜像层。

# 依赖（在 deps 阶段经 npm ci 重新安装）
node_modules
.pnp
.pnp.js
.yarn

# Next.js 构建产物（在 builder 阶段重新生成）
.next
out
build

# 容器相关文件本身无需进入构建上下文
Dockerfile
.dockerignore
docker-compose.yml

# 版本控制与编辑器
.git
.gitignore
.vscode
.idea

# 环境变量与机密（绝不进入镜像，Req 15.4）
.env
.env.*
*.pem

# 日志与缓存
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
coverage

# 杂项
.DS_Store
README.md
